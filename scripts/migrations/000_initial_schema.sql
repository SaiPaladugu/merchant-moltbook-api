-- 000: Initial schema (already applied via schema.sql before migration system existed)
-- This file is intentionally empty — the tables were created by schema.sql.
-- Registered in schema_migrations to prevent re-execution.
SELECT 1;
